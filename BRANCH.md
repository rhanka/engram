# feat/agent-memory-312-obligations — external-host amendment (#312) obligations

Base: `origin/main` 1a723695. Scope: **embedded-local** (external-host activation out of scope until the operating agreement is signed, §12 W-C/ARCH-06; the obligations still bind embedded-local). Test-first; ≤150 lines/commit; selective staging; boxes checked in the commit; no AI attribution; no merge without review + owner gate.

## R1a — contracts (data-only DTOs + shape validator)
- [x] DTOs (§5.10/§5.7/§5.9): `AdminProviderPort`, `AdminBootstrapRequestV1`/`AdminRotateRequestV1`/`AdminRevokeRequestV1`, `AdminEpochReceiptV1`, `AdminOperationRequestV1`, `CanonicalMemoryStoreFactoryV1`+`FencedStoreConstructionV1`, `AttestationSignature`+attestation fields on `OperationalCapabilityReceiptV1`, `admin_provider?` on `MemoryEngineDependenciesV2`.
- [x] `validateAdminOperationRequest` shape gate + RED→GREEN `tests/memory-admin-contract.test.ts`.

## R1b — `MemoryPortV2.admin` entry point + dispatch  [admin-entry DONE · retirement PENDING]
- [x] admin() on MemoryPortV2 + engine dispatch: `CAPABILITY_UNAVAILABLE` when no `admin_provider` (before any other check) · `INVALID_SCHEMA` on malformed request · bootstrap `FENCE_LOST` unless `request.storage_epoch` equals the live fence epoch (before dispatch) · dispatch bootstrap|rotate|revoke per discriminant · provider denial passes through as `UNAUTHORIZED`. `tests/memory-admin-entry.test.ts` (8 cases). Comment-anchors on the 5 non-type-expressible constraints + SHAPE-GATE note on `validateAdminOperationRequest` (g-arch review items 1–2).
- [x] Hardening per g-arch review (a)(b)(c): (a) order test — malformed request **with** no provider still refuses `CAPABILITY_UNAVAILABLE` (proves provider-check precedes schema-check); (b) a throwing `readiness()` maps to `STORE_UNAVAILABLE` (broken store), a not-ok `readiness()` propagates the store's own code, and the engine asserts `FENCE_LOST` only on an epoch mismatch (§5.7 keeps the two codes distinct) + throw path now tested; (c) explicit `revoke` guard + unreachable exhaustiveness branch instead of an unguarded fallthrough.
- [x] (d) CLOSED in R1g (owner-ratified): the engine now maps a provider-declared denial (`error.denial === true`) to `UNAUTHORIZED`; see the R1g section.
- [x] Retire = **B** (g-arch §5.10 read: admin is supplied *by injection* in both modes; credential custody + exclusive console are host obligations "never part of this port's payload" ⇒ a graphify-shipped provider is structurally impossible ⇒ the primitives have no sanctioned consumer). Deleted `graphify-memory/service.ts` (the entire admin family: 5 factories + 6 types) + removed the `./service.js` export block from `index.ts` + the `./service` subpath from `package.json`. By MEMBERSHIP not prefix: `LocalFilesystemProbeResultV1` (sqlite.ts, non-admin) stays. Neutralised a stale `managed-service` comment in `postgres.ts`. The `CAPABILITY_UNAVAILABLE` "rather than shipping a default administrator" refusal is already wired in `admin()` (R1b-core); no default-admin wiring existed to remove. Deleted `tests/local-administrator.test.ts` (its subject is gone) + extended `tests/memory-v1-removal.test.ts` to guard the retired family + `standalone-service`/`managed-service` vocabulary out of `graphify-memory`.
- [ ] DEFERRED → R1g: the AdminProviderPort **conformance** refonte (the §5.10 6-obligation harness that the old local-administrator test's semantics become). Its central assertion (unauthorized op ⇒ `UNAUTHORIZED`) is the OPEN (d) decision — authored once (d) is ruled, to avoid guaranteed rework.

### Conformance note — why `admin()` was deferred from R1a to R1b (g-arch review item 4)
Both ways of making R1a compile with `admin()` present violate the spec, so neither was shipped in R1a:
- **optional `admin?()`** weakens the §5.5 mandate that admin is the single graphify-mediated entry point (dispatch becomes skippable);
- **required `admin()` + a stub** ships a default administrator that §5.10 explicitly retires.
Deferring the method (not just avoiding a tsc break) is therefore the only spec-faithful option — the interface stays consistent with §5.5/§5.10 until the dispatch lands in R1b.

## R1c — `CanonicalMemoryStoreFactoryV1.acquire` (fence-at-construction, single-broker refusal)  [DONE]
- [x] `graphify-memory/store-factory.ts` — `createCanonicalMemoryStoreFactoryV1({adapter_id, adapter_version, open})`. The `open` seam takes the storage fence at construction (graphify wires its own sqlite/postgres openers; the factory never re-implements the broker, §5.7). In-process half of the single-broker rule: a live holder for a store_id refuses `STORE_UNAVAILABLE` (not queued, opener not re-invoked); reservation before opening closes the race; released on graceful `close()`; a failed/throwing acquisition frees the slot (holds no fence). Cross-process half is the opener's flock/generation lock. `tests/canonical-store-factory.test.ts` (6 cases). Exported from `index.ts`.
- Note: attestation binding on the acquired store's `readiness()` receipt is R1d (emission + engine-side verification kept together).

## R1d — capability attestation verification (§5.9)  [predicate DONE · engine wiring PENDING]
- [x] R1d-a — contract `CapabilityAttestationVerifierPort` (expected adapter id/version/build_digest + `verifySignature`) + `attestation_verifier?` on `MemoryEngineDependenciesV2` + pure predicate `verifyCapabilityAttestation(receipt, verifier, operation)` in `graphify-memory/attestation.ts`. Rule: `backend "memory"` admitted with no attestation; production (sqlite/postgres) admitted only when the fresh receipt carries a complete attestation block matching the EXPECTED adapter identity and whose detached signature verifies; absent/mismatched/no-verifier/unverifiable ⇒ `CAPABILITY_UNAVAILABLE` (rejected as an unfenced store). `tests/capability-attestation.test.ts` (7 cases, incl. postgres/external-host). Exported from `index.ts`.
- [x] R1d-b — shared `admitFenced(operation)` helper (fresh readiness receipt → `verifyCapabilityAttestation`, engine-side, never delegated to readiness()) gates the **five** fencing-dependent ops BEFORE they run: capture, requestAdmission, transition, recall, proposeCapitalisation. Per g-arch review: (1) gate is unconditional (§5.9 l.1043 `fenced_single_writer` is not self-asserted) — verdict is production-conditional, memory backend admits with no attestation; (2) `capabilities()` EXCLUDED — a reporting/diagnostic surface, not a fence-dependent op (§5.9 l.1041), and the danger is already covered by (1); (3) `readiness()` called per-op, result NOT memoised (fresh, §5.9 l.1041/1047) — comment forbids adding a cache. Shared `memoryReadinessReceipt()` (memory backend, NO attestation) completes 3 minimal fake stores (memory-capture/authz/admission-envelope). `tests/capability-attestation-gate.test.ts` (3 cases: 5-op refuse on unverified production; admit-past-gate with a valid verifier; refuse on a rejecting verifier). transition/proposeCapitalisation reuse the gated receipt (dropped their redundant inner readiness reads).

## R1e — `graphify-memory/integration` surface (factories/types/ports only)  [DONE]
- [x] `graphify-memory/integration.ts` — neutral host-facing surface (§1 D1): re-exports `createMemoryPortV2` (engine factory), `createCanonicalMemoryStoreFactoryV1` (+ option types), `verifyCapabilityAttestation`, and `export type *` from contracts. Driver-free: the concrete sqlite/postgres openers stay in their own subpaths (host passes one to the factory). Decoupled per Decision B — NO reference administrator/provider factory. `./integration` package subpath added. `tests/integration-surface-neutrality.test.ts` (3 cases: factories are runtime functions; imports only the neutral internal modules — no driver/node/listener/consumer import; no retired local-administration re-export).

## R1f — ABSORBED into R1b-retire (`a939e2ee`)  [DONE]
- The retirement was pulled forward into R1b-retire=B; no separate R1f lot remains.

## R1g — close (d): provider denial normalisation + conformance harness (owner-ratified)  [DONE]
- [x] R1g-a (encoding, mechanism A): `denial?: true` optional on `MemoryErrorV1` (additive, non-breaking) + `normalizeAdminDenial` applied TOTAL to the three admin dispatch results (`error.denial === true ⇒ code = UNAUTHORIZED`, mechanical — the engine maps the provider-declared flag, it does not reclassify). `tests/memory-admin-entry.test.ts` (d)/(e) reworked: a denier `{code: STORE_UNAVAILABLE, denial: true}` normalises to `UNAUTHORIZED`; a non-authorization failer (denial absent) passes through with its own code. Spec §5.5(d) + the §5.5 prose updated to the converged text.
- [x] R1g-b (classification, mechanism B / §10 harness): shippable `graphify-memory/admin-conformance.ts` → `assertAdminProviderConformance(provider, cases)` (caller supplies the denial + non-authorization-failure scenarios since only it can drive an opaque provider; harness verifies the `denial` invariant both directions). Exported from `index.ts` and `integration.ts` (allowlist updated). `tests/admin-provider-contract.test.ts` (4 cases: conformant passes; denial-without-flag rejected; flag-on-non-auth-failure rejected; denial-scenario-succeeds rejected). Spec §5.10 MUST + §10 conformance row updated to the converged text.

## R1h — g-arch R1g review hardenings (non-blocking)  [DONE]
- [x] (a) JSDoc on `assertAdminProviderConformance`: the `{conformant:true}` verdict is scoped to the supplied `cases` only, not a blanket certification of the provider's whole behavior.
- [x] (b) fail-closed on a throwing provider: `callAdminProvider` wraps ONLY the three provider calls (not the surrounding engine/fence logic, so an engine bug is not masked) so a provider that throws returns a typed `POLICY_UNAVAILABLE` ("admin provider failed") — `admin` is now TOTAL on its `Result` contract (previously only `readiness()` throws were caught). g-arch resolved the code: `POLICY_UNAVAILABLE` (the admin provider is the authority of `authorization_epoch`, §5.10), NOT `CAPABILITY_UNAVAILABLE` (which already means "no provider injected" — reusing it would fuse config-error and provider-fault). Three invariants: `retryable:false` (fail-closed); never `UNAUTHORIZED`/`denial:true` (an exception declared no decision); never the raw `String(e)` (D2 minimal redaction — could leak a credential ref/path/DSN), fixed message only. Spec §5.5(f) defines its first use. Test asserts the code, `retryable:false`, no `denial`, and that a secret in the thrown error does not leak into the message.
- [x] (c) symbol-level neutrality: `integration-surface-neutrality` now asserts (module-agnostic) that no value export implements `AdminProviderPort` (`{bootstrap,rotate,revoke}` shape) and no `^create.*Admin(istrator|Provider)` factory is exported — encodes Decision B on the symbols, augmenting the per-module import allowlist.

## Follow-up (tracked, out of R1)
- `graphify-memory/index.ts` re-exports `./sqlite.js` (which imports `fs-ext`), so the MAIN entry is not driver-free — every consumer fails to load without `fs-ext` (not only SQLite users; explains the 2 non-SQLite pre-existing failures). `integration.ts` IS driver-free. Candidate: make the main entry driver-free (lazy / subpath the sqlite re-export). Pre-existing; g-arch-noted; not in R1h.

## R1i — g-arch R1h review closure (5 one-liners, no behavior change)  [DONE]
- [x] #5 (the real one) — `integration-surface-neutrality` symbol guard could pass VACUOUSLY if the namespace emptied (exports moved / import path changed; tsc misses it). Added a positive control (`expect(integration.createMemoryPortV2).toBeDefined()`) before the loop + a comment declaring the limit (factory arm is name-based `/^create.*Admin.../`; behaviour-detection not generic; human review is the net).
- [x] #1 — secret-leak test now also asserts `JSON.stringify(r)` (the WHOLE `Result`) excludes the secret, guarding a future `catch(e){ return {…, detail:String(e)} }`.
- [x] #2 — `callAdminProvider` JSDoc corrected: wraps the provider call AND the normalisation of its result (a throw from either — exception or malformed `Result` — is a provider fault); still NOT the surrounding engine/fence logic.
- [x] #3 — §5.5(f) now reads "throws, or returns a value that is not a well-formed `Result`" (the code already classifies a malformed `Result` as `POLICY_UNAVAILABLE`).
- [x] #4 — §5.10 gains "an `AdminProviderPort` MUST return a `Result` and MUST NOT throw" (grounds §5.5(f); explicit stratification).

## R1j — fix the 4 attestation regressions: emission gap → in-process provenance mark (§5.9)
Independent review found the R1d-b gate verified an attestation NOTHING emitted (production bricked). A first crypto attempt (`2592adb6` verify-side recompute, `a1cecd66` Ed25519 emission) was then found **tautological** (F1: a host-provided signer + host-configured expected identity cannot detect a duplicated adapter; the signature is redundant with `receipt_digest` integrity) + a DI footgun (F4). g-arch reckoned: crypto OUT, an **in-process provenance mark** IN (required by the declared scope — l.1046 threat 3 "never took the fence" needs provenance, not a copyable string; engineering per l.1341, not owner-gated). Forward-transform (not git-revert of the deep R1d-a crypto commit); intermediate commits show the review→fix trail.
- [x] R1j-mark-1 (mechanism): `graphify-memory/store-factory.ts` now owns a module-private `WeakSet` provenance mark — the factory stamps each store it builds after taking the fence — + `isFencedFactoryStoreV1(store)` + `verifyStoreProvenance(store, receipt, operation)` (memory admitted unmarked; production admitted only when (a) built by THIS module's factory AND (b) the receipt's declared identity == compiled `GRAPHIFY_MEMORY_ADAPTER_IDENTITY`) + readiness stamps the declared identity from that constant. Engine `admitFenced` calls `verifyStoreProvenance(canonical_store, …)`. REMOVED: crypto verifier/signer/pair (`CapabilityAttestationVerifierPort`, `CapabilityAttestationSignerV1`, `createCapabilityAttestationPairV1`), `attestationReceiptDigest`, `attestation_signature`, `AttestationSignature`, `attestation_verifier` dep (F6 moot), Ed25519 node-crypto decls, the `attestation.ts` module. Tests: `store-provenance.test.ts` (factory store admitted; non-factory store refused even self-declaring the right identity — the mark, not a string, is the proof; memory admitted), gate test reworked to a factory-built vs bare production store, R1c factory test to `{open}` + membership. No new suite failures (only pre-existing env).
- OPEN NOTE for g-arch review: check (b) — a store either is in THIS module's WeakSet (same module ⇒ identity always matches) or isn't (refused at (a)), so (b) appears **unreachable in-process** (membership subsumes it); keep per spec or drop is g-arch's call.
- [x] R1j-mark-2: spec §5.9 amendment — removed `AttestationSignature` + `attestation_signature` (former l.990/1005), simplified the receipt to a declared adapter identity, rewrote the threat-scope tail + "Single admission path" to the in-process mark + store-independent compiled-identity source + a design-reserved external-host note, and updated the §5.7 factory comment, the normative "verified attestation block" line, and the L4/§10 rows (attestation → provenance). Adapted the native tests: `canonical-memory-store` (both) + `sqlite-memory-broker.native` (all three) now acquire through `factory.acquire` so their store carries the mark; test 2's applyAdmission observer moved INSIDE the factory's `open` seam (wrapping a marked store afterward would drop the mark). Not runnable locally (fs-ext absent → the store open itself fails with "another live process holds the SQLite writer fence"); tsc clean, the adaptation compiles and runs up to the open. **g-arch re-verifies by identity vs main in a clean (fs-ext-present) env.**
- [x] R1j-v5a (gate + mode; engine-side, unit-testable locally): close **F1** and the two holes independent review found in the naïve exemption. `verifyStoreProvenance(store, receipt, {allowUnfencedMemoryStore}, operation)` now decides on the **in-process mark ONLY**, never `receipt.backend`: (1) PRODUCTION — built by THIS module's fenced factory (membership) AND `receipt.capabilities.fenced_single_writer===true`; (2) NON-PRODUCTION EXEMPTION — this module's neutral in-memory store (membership in a new module-private `inMemoryStores` WeakSet, stamped by `createInMemoryCanonicalMemoryStoreV1` via the internal `markInMemoryStoreV1`) **AND** the host raised `allow_unfenced_memory_store`. Both are required (flag alone would exempt any unfenced store incl. a lock-less SQLite; membership alone would let an in-memory store run in prod); everything else refuses (prod fails closed). REMOVED: the `receipt.backend==="memory"` exemption and the (b) declared-identity check (the OPEN NOTE above — membership subsumes it; g-arch confirmed unreachable). Added `allow_unfenced_memory_store?: boolean` to `MemoryEngineDependenciesV2`; engine `admitFenced` passes it. Kept the factory's existing stamping (v5-a greenness; the opener-flock stamping moves to v5-b). `markInMemoryStoreV1` stays INTERNAL — not re-exported from the barrel, `./store-factory` absent from package.json `exports`, so a host cannot reach it (and it is inert without the flag). Tests: `store-provenance.test.ts` covers g-arch's 4 cases — flag∧module⇒admit (1); flag + bare `backend:"memory"`⇒refuse (2); no-flag + module⇒refuse (3); backend-independent decision (4) — plus the two production paths; the non-production fixtures (`memory-l3`/`memory-l7`) opt into the flag, and three bespoke engine stubs (`memory-capture`, `memory-authz`, `memory-admission-envelope`) + the ranking Proxy wrapper are declared embedded in-memory fakes via a test-only `embeddedInMemoryStub`. tsc clean (the 7 pre-existing `src/llm-mesh-bridge.ts` errors are out of scope); full memory suite green except the pre-existing fs-ext-absent native failures.
- [x] R1j-v5b(1) (pure /proc/locks decode+parse+match; locally testable, no fs-ext): new module `graphify-memory/proc-locks.ts` (INTERNAL — not re-exported from the barrel) holds the Linux liveness-proof primitives the fs-ext flock code will call: `decodeDevToProcLocksMajMin(dev)` inverts glibc `gnu_dev_major`/`gnu_dev_minor` and the kernel `%02x:%02x` field with **BigInt masks** (a signed JS `>>` corrupts a minor whose encoded bit 31/32 is set — minor≥2^19 — and `>>>` alone still truncates the >32-bit major bits); `procLocksFileKeyV1(dev, ino)`; `procLocksHoldsExclusiveFlockV1(content, {pid, majMinIno})` (matches a HELD `FLOCK … WRITE` holder line by pid + `maj:min:ino`, skips `-> ` blocked-waiter lines); `parsePidFromProcStatusV1(content)` (the `/proc/self/status` Pid — the namespace-correct pid, since `process.pid` did not match /proc/locks in the container spike). `tests/proc-locks.test.ts` (10/10 green): the decoder cases g-arch demanded — overlayfs major-0 `170→00:aa`, block-device major≠0 `252:1→fc:01`, `259:0→103:00`, `8:17→08:11`, wide minor `252:256→fc:100`, and the signed-shift traps `mkdev(0,0x80000)=0x80000000→00:80000` and `mkdev(253,0x100000)→fd:100000` (bit 32) — plus the matcher/parser. **Real-device cross-check (no docker):** this host's own fs `st_dev=64513=0xfc01`; both Python `os.major/os.minor` (independent glibc-macro oracle) and the decoder yield `fc:01` — the same value g-arch predicted the bind-mount replay would show, so the major-decode path (which the major-0 spike never exercised) is confirmed on a real block device. The docker bind-mount /proc/locks round-trip (flock+parse on a real block dev) remains the final "prod proven" gate and runs in CI/spike with fs-ext present.
- [x] R1j-v5b(1a) (kernel-verified review of proc-locks.ts — g-arch confirmed it against the REAL kernel on a real block dev: major 252, `FLOCK…WRITE fc:01:50247445`, matcher false/true/false, 2nd-fd `EAGAIN`): two corrections. (i) **fdinfo over /proc/locks** — the fence will read `/proc/self/fdinfo/<fd>`, not `/proc/locks`: /proc/locks has (process,inode) granularity, so another fd of the SAME process holding a flock on the inode (an unclosed probe, a prior instance) makes a /proc/locks match a **false positive** even when OUR fd never took the lock; fdinfo attaches the `lock:` record to the exact open-file-description that holds it (closes it by construction, O(1)). `procLocksHoldsExclusiveFlockV1` now strips an optional leading `lock:\t` token and makes `pid` OPTIONAL (fdinfo is already our fd; pid disambiguates only the process-wide /proc/locks). The `lock:` line format equals a /proc/locks record after the prefix — **the presence of `lock:` lines in fdinfo on the OVH MKS kernel is re-verified at the container spike (gate 2).** (ii) **bigint** — `decodeDevToProcLocksMajMin`/`procLocksFileKeyV1` accept `number | bigint` so the caller passes `fstatSync(fd,{bigint:true})` dev/ino directly; a large XFS/btrfs inode routed through a JS `number` loses precision above 2^53 and would break the key match (bricking). (iii) reworded `parsePidFromProcStatusV1` — `/proc/self` is right because it reads the same procfs namespace as fdinfo/locks (not an unverified "spike divergence"). tests/proc-locks.test.ts 12/12 (adds a fdinfo `lock:`-prefixed match with no pid, and a 2^53+1 bigint-ino precision case). The committed CI oracle test uses fs-ext + real fdinfo — **never Python** (owner rule); the earlier local Python cross-check was ephemeral, not committed.
- [x] R1j-v5 hardening (g-arch non-author review of v5-a — two locally-runnable merge-unit items): (MOYEN 1) `tests/memory-fence-default-refusal.test.ts` binds the default-refusal to the ENGINE, not just the pure function — g-arch's mutation (forcing `allowUnfencedMemoryStore: true` in engine.ts) had survived the whole suite because every fixture sets the flag. Over a genuine module in-memory store (WeakSet member), it asserts the 5 fencing ops refuse `CAPABILITY_UNAVAILABLE` when the flag is absent (a) and when it is the forgeable string `"true"` (b, proving the `=== true` strict boolean), and admit past the gate (INVALID_SCHEMA on a bogus payload) only with boolean `true` (c). **Verified the criterion**: forcing the flag true in engine.ts turns (a)+(b) RED (mutation killed), reverted. (BAS) `tests/internal-mark-encapsulation.test.ts` scans `graphify-memory/**` + `src/**` (non-test) and fails if any file other than `store-factory.ts` (defines) / `memory-store.ts` (calls) references `markInMemoryStoreV1` — and asserts those two DO still reference it (non-vacuous). Table-driven, extends to `markFencedStoreV1` in v5-b. Both green; tsc clean. **Merge note (breaking, owner-FYI):** an external host wiring the neutral in-memory store WITHOUT `allow_unfenced_memory_store` now gets `CAPABILITY_UNAVAILABLE` on fencing ops — in-repo consumers: 0; external consumers: source-gap.
- [x] R1j-v5b(2) (sqlite.ts fence mechanism — kernel-proven; not locally runnable without fs-ext): the writer fence is now a non-blocking exclusive flock on the canonical DB's OWN inode (retired the `${filename}.lock` sidecar), taken on a private fd (`acquireDatabaseFlock`); SQLite opens the file on a separate fd, so a second live holder in any process is refused EAGAIN. `#fence` (run before the first write, before COMMIT, and on every read op) now runs THREE checks in order: (1) in-process `assertHeld()` fast flag; (2) **liveness** `assertKernelHeld()` — `/proc/self/fdinfo/<fd>` still shows OUR fd's exclusive flock (fdinfo is per-open-file-description, so it cannot be spoofed by another fd of the same process, and an fs-ext shim cannot forge it); (3) **anti-rename** `assertPathUnmoved()` — the path still resolves to the flocked (dev,ino) captured via `fstatSync(fd,{bigint:true})` (a `mv other db` leaves the old inode locked but the name repointed; fdinfo alone cannot see that, so it is a SEPARATE check); then the existing durable-epoch check. In-process **NFS/network refusal** is enforced against the ACTUAL open file (`statfsSync("/proc/self/fd/<fd>").type` ∈ rejected/⊄ local ⇒ refuse) so a lenient dir probe cannot mask it. **macOS/Windows fail closed** at open (`globalThis.process?.platform !== "linux"` — read off globalThis to avoid a `process` global clashing with @types/node in the repo typecheck). REMOVED the production-reachable `lock_factory` and `filesystem_probe` seams (F1-family bypasses; no test used them). `node-fs.d.ts` shim extended (readFileSync, fstatSync/statSync `{bigint:true}` → `BigIntFileStats`) — package tsc clean AND the overloads merge harmlessly with @types/node in the root tsc (still 7 pre-existing llm-mesh-bridge errors, none in memory scope). Native broker test's competing-holder child now flocks the DB file itself, not the sidecar. In-memory suite green (32/32); the real-fence admit path is verified by g-arch's local fs-ext+kernel probes (P0–P4; SQLite natives 5/5 on `19daa260`) — durable gates (CI at PR, container round-trip gate 2, non-regression vs `main`) remain **pending**, not yet run.
- [x] R1j-v5b(3) (store-factory opener-stamping switch + positive-test-flip): the factory no longer stamps every acquired store — it PROPAGATES the opener's mark to its wrapper (`if fencedFactoryStores.has(raw) then add(marked)`). Both fence-taking openers stamp `markFencedStoreV1(store)` after their real fence: sqlite after the flock+/proc proof+epoch, postgres after the advisory-lock generation. `verifyStoreProvenance` prod path DROPPED the self-declared `fenced_single_writer===true` basis (same forgeable family as F1) and rests solely on membership (kernel-proven) + the store's per-op #fence. `markFencedStoreV1` is INTERNAL (barrel omits it; `./store-factory` not in package `exports`). Tests: store-provenance.test.ts FLIPPED — a factory acquisition whose opener never took a real fence is REFUSED (`isFencedFactoryStoreV1` false; §5.9 threat-3 store), the admit case models the opener stamping (propagation), and a case proves admission rests on the MARK even when the receipt self-declares `fenced_single_writer:false`; capability-attestation-gate.test.ts likewise flipped (unstamped factory acquisition ⇒ CAPABILITY_UNAVAILABLE) + an engine-level admit resting on a stamped opener. internal-mark-encapsulation extended for `markFencedStoreV1` (allowed: store-factory.ts defines; sqlite.ts + postgres.ts call). Package tsc clean; root unchanged (7 pre-existing). In-memory suite 53 pass/1 skip. NOTE: this also fixes a latent issue — a directly-opened postgres store (its native test opens without the factory, and is env-gated to skip) was refused by the gate since the R1j mark; opener-stamping admits it now. Real-fence admit verified by g-arch's local probes; durable CI/container/non-regression **pending**.
- [x] R1j-v5d (spec §5.9 amended to the implemented mechanism; lands WITH v5-b): rewrote the §5.9 attestation subsection — threat scope, *Single admission path (in-process fence mark)*, and the renamed *Non-production exemption* — to: the opener stamps the store ONLY after a real kernel fence and the factory propagates it (retired "factory stamps every store"); the prod check is membership alone (retired the compiled-identity comparison AND the self-declared `fenced_single_writer` basis, both called out as the same forgeable family as `backend`); the SQLite fence = flock on the DB inode + `/proc/self/fdinfo` liveness + separate anti-rename + fd-`statfs` network refusal + `storage_epoch`, re-proven per op, **Linux-only** (macOS/Windows fail closed — declared gap); the memory exemption = `allow_unfenced_memory_store` (strict boolean) ∧ module in-memory membership, and the decision **never consults `receipt.backend`** (F1). Updated the `OperationalCapabilityReceiptV1` identity comment (informational/diagnostic only), the `CanonicalMemoryStoreFactoryV1` DTO comment (propagates the opener's mark), the §8/D8 lock-helper line (l.1253: flock-on-inode, no sidecar, no `LockFileEx`, Linux-only — declared gap) + a **Kubernetes deployment constraint** (block-`storageClassName` PVC, RWO, reject an `nfs.csi.k8s.io` provisioner; NFS/CephFS a follow-up), the L4 gate row (mechanism + the new test names + a **durability note**: the Linux native lane MUST NOT be skipped or the sole positive proof vanishes), and the C11 conformance row + the closing decisions paragraph. Prose-only; no code.

- [x] R1j-v5c (posture legibility; locally verified): the `allow_unfenced_memory_store` fence-bypass posture is no longer silent. Added `unfenced_memory_store_permitted: boolean` to `CapabilityDescriptorV1`, populated by `capabilities()` from `dependencies.allow_unfenced_memory_store === true` — so an operator querying a sqlite/postgres descriptor sees a `true` there as a **production misconfiguration alarm** (the bypass flag set in prod). Added an optional `on_unfenced_memory_store_permitted?: () => void` engine dependency (the package emits no console; observability is an injected callback, per the `on_mutation_statement` convention) fired **once at construction** when the flag is set, covering the memory case (where `capabilities()` is unavailable). tests/memory-fence-posture.test.ts (3 green): descriptor reflects the strict boolean (string `"true"` ⇒ false), the hook fires once when set and never when absent/false/string. No existing test asserted the descriptor, so the added digest field is non-breaking.

- [x] R1j-v5b(4) (g-arch empirical review P0–P4 — HAUT + regression + MOYEN 1, before merge): (HAUT/P1) `acquireDatabaseFlock` now proves the flock is really recorded by the kernel — `assertKernelHeld()` + `assertPathUnmoved()` — **immediately after `flockSync`, before returning**, throwing otherwise. Previously it returned unconditionally and the opener then advanced the durable epoch + stamped; a non-functional `fs-ext` (shim/misconfig) whose `flockSync` returns success without a real lock would advance the epoch while a legitimate broker holds the real flock elsewhere, driving THAT broker's next `#fence` to a spurious `FENCE_LOST` (killing the live writer). Now the fake is refused before any write. (MOYEN 1) The open catch maps **only** genuine flock contention (`EAGAIN`/`EWOULDBLOCK`) to `FENCE_LOST`; a missing/non-functional native module, a network filesystem, an unreadable `/proc`, or the kernel-proof refusal now yield `CAPABILITY_UNAVAILABLE` **with the cause** — the earlier catch-all `FENCE_LOST` had misreported an unfenceable env (fs-ext absent) as a lost fence (and misled my own "env baseline" reading of the return code). (regression, bisected to `e2aecde1`) the native `canonical-memory-store` "same id…" test wraps the store in an observing Proxy; the opener stamps the RAW store, so the Proxy was unmarked and the gate refused it — the test's opener now propagates the mark (`if (isFencedFactoryStoreV1(raw)) markFencedStoreV1(observing)`). Added the pre-registered P1 harness `tests/sqlite-fence-shim.native.test.ts`: `vi.mock` on the resolved `fs-ext` path (no-op flock) + a pre-seeded epoch ⇒ open refused `CAPABILITY_UNAVAILABLE` AND epoch unchanged. Package + root tsc clean; in-memory suite green (36/36). g-arch re-runs P0–P4+P1 on the fix. After-merge follow-ups tracked in R2 (MOYEN 2 drop `dev` from the fdinfo match; BAS refuse `nlink>1`, document `release()` after `database.close()`).

- [x] R1j-v5b(5) (g-arch MOYEN 2 — pulled INTO the v5 unit, before the gates): the fdinfo/`/proc/locks` liveness match now keys on the **inode only**, never the device. The kernel prints the superblock `s_dev`, while `fstatSync(fd).dev` is the mount/subvolume `st_dev` — on **btrfs** the subvolume anon-dev differs from `s_dev`, so a `maj:min` match would spuriously fail and **brick the fence on btrfs** (and would brick the gate itself if the CI runner or container gate-2 is btrfs). The inode `i_ino` is identical in both views, so an inode match is device-agnostic; and since fdinfo is per-fd, a `FLOCK WRITE` record there already concerns our file (the inode is a cheap sanity check). Removed the now-dead `decodeDevToProcLocksMajMin` and `procLocksFileKeyV1` (the device decoder — the "bricking-prone" glibc `gnu_dev_*`/`%02x:%02x` logic — and their unit tests); `procLocksHoldsExclusiveFlockV1(content, { ino, pid? })` extracts the inode as the last colon field of `maj:min:ino`. sqlite.ts passes `fstatSync(fd,{bigint:true}).ino.toString()` (exact for a >2^53 inode). proc-locks.test.ts 8/8 (adds a btrfs case — same inode under a different `maj:min` still matches — and a >2^53 inode exact-decimal case). Package + root tsc clean. (Info from conductor: GitHub ubuntu-latest ≈ ext4, gate-2 bind-mount = ext4 host `fc:01`; MOYEN 2 removes the dependency on that fact.)

- [x] R1j-v5b(6) (g-arch decision (5) — refuse ephemeral filesystems in production; pulled into the v5 unit before merge): the fenced SQLite store refuses an **ephemeral** filesystem (tmpfs `0x01021994`, overlayfs `0x794c7630` — a DB there is lost on pod/container restart, contradicting the block-PVC constraint) unless the host sets a NEW **dedicated** strict-boolean option `allow_ephemeral_filesystem_store` (distinct from `allow_unfenced_memory_store` — a memory flag must not widen a filesystem decision; the F1 lesson). New PURE module `graphify-memory/filesystem-fence.ts` (`classifyFenceFilesystemV1` + the three magic-number sets) so the verdict is unit-testable without fs-ext; the fd-`statfs` check in `acquireDatabaseFlock` calls it (one set-read, tmpfs and overlayfs in the SAME ephemeral set — same code path; the real overlay behaviour is a G2 concern). Refusal is `CAPABILITY_UNAVAILABLE` naming the fs type in HEX. Strict `=== true` (a string `"true"` is refused — the mutation-surviving case (b) discipline). Cleaned the fs list: removed apfs/hfs+/ntfs/refs (unreachable under Linux-only). Posture surfaced in `readiness()` (store caps `ephemeral_filesystem_store_permitted`) and `capabilities()` (engine descriptor); DTOs (`CanonicalStoreCapabilitiesV1`, `CapabilityDescriptorV1`) aligned in code AND spec §4-§5. Native fixtures (canonical/broker/shim opens) declare the flag so they pass on a tmpfs `/tmp` (Fedora) regardless of machine. Spec §5.9: refusal + fs-list cleanup + the **declared residual** (a durable fs does not guarantee a single *logical* store — two nodes/PVCs still diverge; rests on D-A single-node + RWO block PVC). Tests: `tests/sqlite-filesystem-classification.test.ts` (pure — tmpfs+overlayfs refused without flag / admitted with; network never overridden by the flag; unknown refused; non-vacuous) + posture reflection in `memory-fence-posture.test.ts`. Package + root tsc clean; in-memory green. The string-`"true"` rejection and the real tmpfs/overlayfs refusal are g-arch's native/G2 checks.

### Gates BEFORE merge (v5 unit — must pass; NOT optional)
- [ ] G1 — g-arch non-author re-probe (P0–P4 + P1) on the branch head `23d9d074` (the current tip, incl. MOYEN 2). Prior probe on `41b16df8` closed HAUT (P1 v2: shim + other-process-real-flock + epoch 7→7, broker intact), 34/34 green, non-vacuous (mutation M1 ⇒ red).
- [ ] G2 — **container round-trip (gate 2), a MERGE BLOCKER, not an R2 follow-up:** confirm that `/proc/self/fdinfo/<fd>` actually contains `lock:` records on the target OVH MKS kernel, with fs-ext present, on a real block bind-mount. If fdinfo does NOT surface `lock:` lines there, `assertKernelHeld()` returns false on every op ⇒ every fencing op ⇒ `FENCE_LOST` ⇒ production is bricked. This must pass before merge. (MOYEN 2 made the match device-agnostic, so the fs type no longer matters; the `lock:` presence still does.)
- [ ] G3 — CI at the PR: all test lanes green on Linux, native SQLite lane NOT skipped (the sole positive proof of the real-fence admit path).
- [ ] G4 — non-regression by identity vs `main` (SLOT), then owner-gated merge via the conductor's presentation.

### R2 — follow-ups deferred out of the v5 merge unit (tracking)
- [ ] R2-a: macOS/Windows production support for the fenced SQLite store — the fence proof reads Linux `/proc` (`/proc/self/fdinfo/<fd>`, `/proc/self/fd/<fd>`), so non-Linux currently fails closed at open. Needs a platform-native liveness+identity mechanism (Windows `LockFileEx` + a handle-identity query; macOS `flock` + a fd-identity path) and its own native suite.
- [ ] R2-b: shared-filesystem canonical store (NFS/CephFS) — a network-FS fence needs a lease / lock-manager mechanism, not a local `flock`; currently refused in-process. Out of scope of the single-node local-block model (D-A).
- [ ] R2-c: `external-host` cross-process provenance — an in-process mark does not cross a process boundary; a cross-process store would need a different (deployment-topology) attestation. Design-reserved in §5.9.
- [~] R2-d → PROMOTED to a pre-merge gate (G2 above): it is a merge blocker, not a deferred follow-up.
- [x] R2-e → done as v5-b(5) (g-arch MOYEN 2 pulled into the v5 unit; see above).
- [ ] R2-f (g-arch BAS, after merge): refuse `nlink > 1` at open. NOT for anti-rename (a hardlink does not defeat the P3 `(dev,ino)` rename check — the inode is the same). The real motive: SQLite derives its `-wal` and `-journal` sidecar names from the DB **path**, so two hardlinks to one inode give two sidecar sets; a crash then a reopen via the other link can lose the WAL and corrupt the database. Also document that `close()` orders `release()` (flock `un` + `closeSync` of the fence fd) AFTER `database.close()` — make the ordering explicit in a comment (it already holds).
